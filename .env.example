# Root .env is optional for this repo.
# Auth secrets for local Worker/dev live in ONE place:
#   apps/workers-api/.dev.vars
#   (copy from apps/workers-api/.dev.vars.example)
#
# Web SPA only needs apps/web/.env (VITE_API_URL).
