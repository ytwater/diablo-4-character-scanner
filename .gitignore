# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
.pnp
.pnp.js

# testing
coverage

# next.js
.next/
out/
next-env.d.ts

# nitro
.nitro/
.output/

# expo
.expo/
expo-env.d.ts
apps/expo/.gitignore
apps/expo/ios
# apps/expo/android is intentionally tracked for the OCR phase0 spike
# (spike/d4-ocr-phase0) so the expo-build-properties native changes
# (build-tools 36.1 pin) are committed. Restore this ignore once the
# spike concludes and the branch returns to normal Expo practice.

# production
build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
dist/
.cache

# turbo
.turbo

# tanstack
.tanstack

# cloudflare
.wrangler
.dev.vars
packages/db/local.db
packages/db/local.db-journal
# Item tooltip photo corpus: regenerate by staging temp-items/ (see
# ItemFixtureCaptureTest). The captured OCR text is committed as
# __fixtures__/item-photo-frames.json; the source photos are not.
apps/expo/test-fixtures/items/
apps/expo/android/app/src/androidTest/assets/
